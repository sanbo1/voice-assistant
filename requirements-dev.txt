# -*- coding: utf-8 -*-
# PC での開発・テスト用（requirements.txt と合わせて入れる）。間接的な依存も含めて固定する。
#
# 固定したバージョンが入手・インストールできなくなった場合は、該当する行の固定を外して新しい版を入れ、
# テストが通ることを確認してからこのファイルを更新する。

# 直接使うもの
pytest==9.1.1

# 間接的な依存
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
colorama==0.4.6; sys_platform == "win32"
