# -*- coding: utf-8 -*-
# Pi 上の venv に入れるパッケージ。間接的な依存も含めて、動作確認したバージョンに固定する。
#
# 固定したバージョンが入手・インストールできなくなった場合（公開停止、新しい OS や Python 向けの
# 配布がない など）は、該当する行の固定を外して新しい版を入れ、動作確認し直してからこのファイルを更新する。

# 直接使うもの
sounddevice==0.5.6
numpy==2.4.6
python-dotenv==1.2.3
openwakeword==0.6.0
onnxruntime==1.30.0

# 間接的な依存（sounddevice → cffi → pycparser）
cffi==2.1.1
pycparser==3.0

# 間接的な依存（openwakeword）
scipy==1.17.1
scikit-learn==1.9.1
joblib==1.6.0
cloudpickle==3.1.2
narwhals==2.26.0
threadpoolctl==3.7.0
tqdm==4.70.1
requests==2.34.2
urllib3==2.8.0
idna==3.20
charset-normalizer==3.5.1
certifi==2026.7.22
# openwakeword が Linux で必ず入れる。このプロジェクトでは使わない（推論は onnxruntime）。
# Python 3.12 以降向けの配布がないため、新しい OS では入らない可能性がある（docs/setup.md 参照）
tflite-runtime==2.14.0; platform_system == "Linux"

# 間接的な依存（onnxruntime）
flatbuffers==25.12.19
packaging==26.3
protobuf==7.36.2
